import { AddNewLinesPipe } from './add-new-lines.pipe';

describe('AddNewLinesPipe', () => {
  it('create an instance', () => {
    const pipe = new AddNewLinesPipe();
    expect(pipe).toBeTruthy();
  });
});
